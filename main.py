from clean_tabular_data import clean_dataframe_data
from clean_images import clean_image_data


if __name__ == "__main__":
    # Clean dataframes
    # images_df = clean_dataframe_data('Images.csv')
    # products_df = clean_dataframe_data('Products.csv')

    # Clean images (Standard size)
    # images_directory = "images/"
    # clean_image_data(images_directory)
    pass